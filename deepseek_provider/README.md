# deepseek_provider

DeepSeek V3 and R1 models. Very fast, very cheap. Requires `DEEPSEEK_API_KEY` in `.env`.

**Models:** `deepseek-chat` (V3, general) · `deepseek-reasoner` (R1, reasoning/math/code)

**Usage:** `switch_model("deepseek-chat")`
